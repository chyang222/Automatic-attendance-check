<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gachon.chan.term1">

    <!-- 네트워크 권한 추가 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Term1"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".check_weeklyActivity"/>
        <activity android:name=".check_attendance_statueActivity"/>
        <activity android:name=".Check_attendance1Activity" />
        <activity android:name=".Check_attendance2Activity" />
        <activity android:name=".Introduce" />
        <activity android:name=".main_activityActivity" />
        <activity android:name=".UploadActivity" >
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>